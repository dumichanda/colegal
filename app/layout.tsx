import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Legal AI Assistant - South African Legal & Compliance Platform",
  description: "AI-powered legal analysis and compliance monitoring for South African law",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.className} dark bg-slate-950 text-white`}>{children}</body>
    </html>
  )
}
